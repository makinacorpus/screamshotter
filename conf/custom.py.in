# Custom settings
TIMEOUT = ${TIMEOUT}

if "${SENTRY_DSN}" != "":
    SENTRY_DSN = '${SENTRY_DSN}'
if "${SENTRY_ENVIRONMENT}" != "":
    SENTRY_ENVIRONMENT = '${SENTRY_ENVIRONMENT}'
if "${SENTRY_TRACE_SAMPLE}" != "":
    SENTRY_TRACE_SAMPLE = '${SENTRY_TRACE_SAMPLE}'
